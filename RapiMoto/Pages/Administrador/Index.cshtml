@page
@model RapiMoto.Pages_Administrador.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Administrador[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Administrador[0].Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Administrador[0].NumeroTelefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Administrador[0].correo)
            </th>
            <th> tipo admin</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Administrador) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroTelefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.correo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
